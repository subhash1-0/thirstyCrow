// Forward declarations for org.mpisws.p2p.transport
#pragma once


namespace org
{
    namespace mpisws
    {
        namespace p2p
        {
            namespace transport
            {
                class ClosedChannelException;
                struct ErrorHandler;
                struct ListenableTransportLayer;
                struct MessageCallback;
                struct MessageRequestHandle;
                struct P2PSocket;
                struct P2PSocketReceiver;
                struct SocketCallback;
                struct SocketCountListener;
                struct SocketRequestHandle;
                struct TransportLayer;
                struct TransportLayerCallback;
                struct TransportLayerListener;
                struct TransportLayerListenerConstants;
            } // transport
        } // p2p
    } // mpisws
} // org
